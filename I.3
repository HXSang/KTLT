#include<stdio.h>

int coso2(long int n)
{
    if(n < 2)
    {
        printf("%d",n);
        return 0; 
    }
    int d;
    d = n % 2;
    coso2(n/2);
    printf("%d",d);
}
int main()
{   
    long int  n;
    printf("Nhap so can bieu dien : ");
    scanf("%ld",&n);
    coso2(n);
}
